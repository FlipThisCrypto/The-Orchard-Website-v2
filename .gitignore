# dependencies / build
node_modules/
dist/
.astro/
.output/
.vercel/
.wrangler/

# Secrets, keys & wallets — NEVER commit (a leaked key is catastrophic for a crypto project)
.env
.env.*
!.env.example
*.pem
*.key
*.keystore
*.p12
*.pfx
*.csr
id_rsa*
id_ed25519*
*.ppk
*.mnemonic
*.seed
mnemonic*.txt
wallet.json
wallet-*.json
keyring*
credentials*.json
secrets*.json
.chia/

# Private operating package — candid strategy + opsec (SPOF, deploy internals, unratified
# tokenomics). Local/private asset only; must NOT be published to the public repo.
operating-system/
# Claude Code project config + skills — contain deploy/box internals; keep private.
.claude/
__wv/
__home/
__tracker.html

# os / editor
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/
